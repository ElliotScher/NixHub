{ config, pkgs, lib, inputs, ... }:

{
  # Host-specific home-manager overrides for elliotscher on mine go here.
  # Anything in ../../../users/elliotscher/home.nix marked with lib.mkDefault
  # can be overridden with a plain assignment.
}
