{ config, pkgs, lib, inputs, ... }:

{
  # Host-specific home-manager overrides for mine go here.
  # Anything in ../../home/elliotscher.nix marked with lib.mkDefault can be
  # overridden with a plain assignment.
}
